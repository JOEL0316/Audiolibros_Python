{
  "name": "traductor-texto-audio",
  "version": "1.0.0",
  "private": true,
  "description": "Audiolibros desde PDF — PWA web + API TTS opcional",
  "scripts": {
    "dev": "npm run dev --prefix client",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "cd server && python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "build": "npm run build --prefix client",
    "preview": "npm run preview --prefix client",
    "install:all": "npm install --prefix client && pip install -r server/requirements.txt"
  }
}
